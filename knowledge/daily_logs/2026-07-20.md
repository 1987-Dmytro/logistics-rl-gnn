---
type: daily_log
date: 2026-07-20
tags: [daily]
---

# 2026-07-20

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->

## Sessions
- 13:13: session ended (auto)
- 13:16: session ended (auto)

## Checkpoint 13:16
- `/init`: no code map was generated — `src/` is empty, there is no architecture yet (removal test +
  the ban on inventing). Instead the `CLAUDE.md` section "Tests" → "Commands (dev)" was filled with real
  invocations from `pyproject.toml` (dev-install, single-test, `--cov`, `ruff check/--fix/format`).
- The `/init` fold-in placeholder anchor (CLAUDE.md:45-46) will hold the code map once code exists.
- 13:18: session ended (auto)

## Checkpoint 17:25
- The RL problem statement is fixed → [[0001-mdp-spec]] (`decisions/0001-mdp-spec.md`):
  dynamic CVRPTW, delivery to the pharmacies of Augsburg on the real OSM network, with
  `[REAL]`/`[ASSUMED]` tags on every input. Frontmatter per the decision template, name `0001-*` as asked.
- The repo's first commit `835b43c` (scaffold + spec). `/commit-commands:commit` failed on an empty
  repo (`git diff HEAD` on an unborn branch) — done by hand: removed a stuck `index.lock`,
  added a minimal `.gitignore` (data/, *.pt, wandb/ — protecting prohibition #1). No push.
- 17:15: session ended (auto)
- 17:24: session ended (auto)
- 17:26: session ended (auto)
- 18:03: session ended (auto)
- 18:10: session ended (auto)

## Checkpoint 18:11
- Phase 1 closed: the package skeleton `src/logistics_rl_gnn/` (data/env/models/baselines/train/
  config/utils — docstring-only), a smoke test, `.pre-commit-config.yaml` (ruff), a `justfile`,
  `README.md` (the mission from [[0001-mdp-spec]] + the dependency pinning plan, CPU index-url torch/PyG).
  No runtime dependencies. The verifier is green (install+pytest+ruff check+ruff format --check).
- A trap: `ruff check .` uncovered 6× E501 + 4 format deltas in third-party `scripts/*.py` (brain tooling,
  never linted before). By the operator's decision they were fixed: `ruff format scripts/` + wrapping 2 long
  f-strings via implicit concatenation (behaviour-preserving, py_compile + a read-only run OK).
- Commit `e711c37` (Phase 1 + the scripts lint fix + checkpoints). The repo has 2 commits, no push.
- 18:12: session ended (auto)
- 18:33: session ended (auto)
- 18:44: session ended (auto)
- 18:47: session ended (auto)
- 19:03: session ended (auto)
- 19:04: session ended (auto)
- 19:15: session ended (auto)

## Checkpoint 19:15
- Phase 2 closed (commit `d7498df`): the Augsburg OSMnx pipeline. `data/osm.py` (the drive graph →
  the strongly connected core, speed imputation with the German fallback + the maxspeed coverage %,
  pharmacies, depot geocoding, snapping, all-pairs matrices), `data/snapshot.py` (graphml+parquet+meta,
  offline load), `config/data.py`, `scripts/build_snapshot.py`, `tests/test_data.py`. The group
  `[data]` in pyproject (osmnx 2.1, CPU). A real run: 4819 nodes, maxspeed 89.7%, 62 pharmacies.
- A fix from the operator's review: `build_matrices` was indexed by `set(nearest_nodes)` → it collapsed
  co-located pharmacies (61×61). Reindexed BY STOP: the depot + every pharmacy get their own row
  (63×63 = 1+62); co-located ones (2 pairs) → Δ=0 but separate rows. + a collision log, assert dim, 2 tests.
- Two bugs caught: (1) a latent Phase 1 one — the `.gitignore` pattern `data/` silently ignored the package
  `src/.../data/` (`__init__.py` was never in git); fixed with the `/data/` anchor. (2) OSMnx writes a raw
  cache into `./cache/` (prohibition #1) → `/cache/` was added.
- The verifier is green (install+build_snapshot+pytest 7 passed+ruff). The repo has 3 commits, no push.
- 19:20: session ended (auto)
- 19:44: session ended (auto)
- 19:50: session ended (auto)
- 19:59: session ended (auto)
- 20:01: session ended (auto)
- 20:14: session ended (auto)
- 20:46: session ended (auto)
- 20:57: session ended (auto)
- 21:34: session ended (auto)
