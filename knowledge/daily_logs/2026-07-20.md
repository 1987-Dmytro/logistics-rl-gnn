---
type: daily_log
date: 2026-07-20
tags: [daily]
---

# 2026-07-20

## Сводка
<!-- что сделано (конкретные результаты), решения, блокеры; связи — [[wikilinks]] -->

## Sessions
- 13:13: session ended (auto)
- 13:16: session ended (auto)

## Checkpoint 13:16
- `/init`: код-карту не генерировал — `src/` пуст, архитектуры нет (removal-test + запрет
  сочинять). Вместо этого дополнил `CLAUDE.md` секцию «Тесты» → «Команды (dev)» реальными
  инвокациями из `pyproject.toml` (dev-install, single-test, `--cov`, `ruff check/--fix/format`).
- Placeholder-якорь `/init` fold-in (CLAUDE.md:45-46) наполнится код-картой, когда появится код.
- 13:18: session ended (auto)

## Checkpoint 17:25
- Зафиксирована постановка RL-задачи → [[0001-mdp-spec]] (`decisions/0001-mdp-spec.md`):
  динамический CVRPTW, доставка по аптекам Аугсбурга на реальной OSM-сети, метки
  `[REAL]`/`[ASSUMED]` у каждого входа. Frontmatter по шаблону decision, имя `0001-*` по запросу.
- Первый коммит репо `835b43c` (scaffold + spec). `/commit-commands:commit` упал на пустом
  репо (`git diff HEAD` при unborn-ветке) — сделал вручную: убрал залипший `index.lock`,
  добавил минимальный `.gitignore` (data/, *.pt, wandb/ — защита запрета №1). Без push.
- 17:15: session ended (auto)
- 17:24: session ended (auto)
- 17:26: session ended (auto)
- 18:03: session ended (auto)
- 18:10: session ended (auto)

## Checkpoint 18:11
- Phase 1 закрыта: пакетный скелет `src/logistics_rl_gnn/` (data/env/models/baselines/train/
  config/utils — docstring-only), smoke-тест, `.pre-commit-config.yaml` (ruff), `justfile`,
  `README.md` (миссия из [[0001-mdp-spec]] + план пиннинга депсов, CPU index-url torch/PyG).
  Без рантайм-зависимостей. Верификатор зелёный (install+pytest+ruff check+ruff format --check).
- Ловушка: `ruff check .` вскрыл 6× E501 + 4 format-дельты в чужих `scripts/*.py` (brain-тулинг,
  ранее не линченный). По решению оператора — починил: `ruff format scripts/` + перенос 2 длинных
  f-string через implicit-конкатенацию (behavior-preserving, py_compile + read-only прогон ок).
- Коммит `e711c37` (Phase 1 + lint-фикс scripts + чекпойнты). Репо на 2 коммита, без push.
- 18:12: session ended (auto)
- 18:33: session ended (auto)
- 18:44: session ended (auto)
- 18:47: session ended (auto)
- 19:03: session ended (auto)
- 19:04: session ended (auto)
- 19:15: session ended (auto)

## Checkpoint 19:15
- Phase 2 закрыта (коммит `d7498df`): OSMnx-пайплайн Аугсбурга. `data/osm.py` (граф drive →
  сильно-связное ядро, импутация скоростей с немецким фолбэком + %покрытия maxspeed, аптеки,
  geocode депо, снаппинг, all-pairs матрицы), `data/snapshot.py` (graphml+parquet+meta, load
  offline), `config/data.py`, `scripts/build_snapshot.py`, `tests/test_data.py`. Группа
  `[data]` в pyproject (osmnx 2.1, CPU). Реальный прогон: 4819 узлов, maxspeed 89.7%, 62 аптеки.
- Правка по ревью оператора: `build_matrices` индексировалась по `set(nearest_nodes)` → схлопывала
  со-узловые аптеки (61×61). Переиндексировано ПО СТОПАМ: депо + каждая аптека = своя строка
  (63×63 = 1+62); со-узловые (2 пары) → Δ=0, но отдельные строки. + collision-лог, assert dim, 2 теста.
- Два бага пойманы: (1) латентный Phase-1 — паттерн `.gitignore` `data/` тихо игнорил пакет
  `src/.../data/` (`__init__.py` не был в git); чинено анкором `/data/`. (2) OSMnx пишет сырой
  кэш в `./cache/` (запрет №1) → добавлен `/cache/`.
- Верификатор зелёный (install+build_snapshot+pytest 7 passed+ruff). Репо на 3 коммита, без push.
- 19:20: session ended (auto)
- 19:44: session ended (auto)
- 19:50: session ended (auto)
- 19:59: session ended (auto)
- 20:01: session ended (auto)
- 20:14: session ended (auto)
- 20:46: session ended (auto)
