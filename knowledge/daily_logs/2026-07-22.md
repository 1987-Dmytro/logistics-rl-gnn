---
type: daily_log
date: 2026-07-20
tags: [daily]
---

# 2026-07-20

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->

## Sessions
- 03:41: session ended (auto)
- 07:49: session ended (auto)
- 09:16: session ended (auto)
- 09:28: session ended (auto)
- 09:28: session ended (auto)
- 09:40: session ended (auto)
- 09:41: session ended (auto)
- 09:43: session ended (auto)
- 10:26: session ended (auto)
- 11:02: session ended (auto)
- 11:08: session ended (auto)
- 11:12: session ended (auto)
- 11:25: session ended (auto)
- 12:20: session ended (auto)
- 12:20: session ended (auto)
- 12:21: session ended (auto)
- 12:22: session ended (auto)
- 12:28: session ended (auto)
- 12:35: session ended (auto)
- 12:57: session ended (auto)
- 13:17: session ended (auto)
- 13:23: session ended (auto)
- 13:50: session ended (auto)
- 14:23: session ended (auto)
- 14:36: session ended (auto)
- 14:55: session ended (auto)
- 14:57: session ended (auto)
- 15:00: session ended (auto)
- 15:35: session ended (auto)
- 16:16: session ended (auto)
- 16:26: session ended (auto)
- 16:54: session ended (auto)
- 17:07: session ended (auto)

## Checkpoint 17:12

**Phase 6b Step 1·refit + Step 2 (congestion training) — both run on the server (base-node), committed/pushed.**

- **Step 1·refit** (anti-overfit POMO): a seed split train/val/test, entropy β=0.01, early-stop best-by-val,
  a train probe (an apples-to-apples gap), an instance freshness hash. The run (early-stop ep23): the "after" **783.2€**
  (gap OR +28.2%, greedy −5.1%). It did **NOT beat** the previous 770.4€ (+1.7%) — but generalisation is confirmed
  (train/val/test/deploy agree → no memorisation). The value = validating the win, not the number.
  `policy_pomo_best.pt` (770.4€) remains the deployment model. [[0006-pomo-static]].
- **Step 2** (Path A: congestion-static POMO + a warm start from 770.4€): vectorising the travel matrix
  (`TravelModel.matrix()` — the k² loop choked the retrain; free-flow bit for bit); a congestion env (incidents on
  **t0**, coverage 100% of nodes); reward+greedy congestion-aware; a **warm-start floor** (deployment ≥ warm start).
  The run (early-stop ep30, best ep15): statics **−1.7%** (RL-cong vs RL-ff under congestion, it restored
  greedy parity+), dynamics (0004 re-plan) **−0.4%** (weak but not an outlier: paired 16:7 events, median
  −4.84€). Under dynamics RL does NOT overtake greedy; the ×134 latency is intact. The floor → no regression.
  [[0007-phase6b-congestion-training]].
- **Infra**: origin (the base-node bare repo) added + pushed; the train-on-server runbook exercised; a persistent
  Monitor for server runs (robust to ssh flakiness). Commits `fe67766`..`63c3fd3`, pytest 70 passed.
- **The key lesson** (in [[0007-phase6b-congestion-training]]): the diurnal is nearly invisible to the encoder (the
  max norm erases a uniform `c`, channel1=`c`≈time_context) → **the incidents on t0 are the whole congestion signal**;
  the design: encode ONCE into the t0 snapshot + the decoder's `time_context` runs over the steps.
- 17:14: session ended (auto)
- 17:58: session ended (auto)

## Step 3 — inference search (19:40)

**Phase 6b Step 3 (NO training, decode only) — built, tested, measured, committed.**

- **Built**: a batched decode (`decoder.logits_batch`, `policy.sample_k` — one encode + a decode over
  K, its own generator → determinism, `temperature>0`), `take_best` (the single scorer), `PortfolioPlanner`
  (candidates sample-K ∪ RL-multistart ∪ greedy → best; **the ≤ greedy guarantee via the byte-identity**
  of the greedy candidate), wiring `rl_planner` into the 0004 harness, `scripts/run_search.py` (3 measurements +
  provenance). 7 new tests, **pytest 77 passed**, ruff clean.
- **The dynamic floor is CLOSED** (the main win): the portfolio is ≥ greedy at **25/25 events** by construction
  (0/25 violations, the worst Δ +0.00€); the 0004 re-plan is **−0.9%** vs greedy (in 0007 it was **+1.6% WORSE**);
  latency **430ms <1s** (OR-Tools ×5). [[0008-phase6b-inference-search]].
- **Statics — modest**: portfolio 766.1€ (−7.2% vs greedy, +25.4% vs OR 611). The discriminator:
  sample-K (the new lever) gave **−2.45% on top of** the pre-existing multistart (785.3→766.1), beating best.pt
  770.4. The heavy lifting is carried by multistart, not by inference search → a marginal plus, not a breakthrough.
- **The key lesson**: the latency of inference search is **env-bound, NOT neural-bound** — the batched decode keeps
  the network flat in K, only K× `env.step` per step grows → ~linear in K (K=256 >1s) → dynamics at K≤32.
- Provenance: `results/search_summary.json` (sha `24c8cfb0607235f8` = 0007, outside git). The vault lesson
  is recorded in [[0008-phase6b-inference-search]].
- 18:18: session ended (auto)
- 18:58: session ended (auto)
- 19:00: session ended (auto)
- 19:18: session ended (auto)
- 19:26: session ended (auto)

## Step 3.5 — local-search polish (21:30)

**Phase 6b Step 3.5 (NO training) — built, adversarially checked, measured, committed.**

- **Built**: `check_feasible` (hard as the env, a mirror of the time-walk), `local_search.polish` (2-opt+
  Or-opt(1-3) intra, relocate+swap inter; a full eval per move → correct under time-dependence;
  the invariant ≤ the input), integration into `PortfolioPlanner` (the top-M in a shared budget, the ≤ greedy guarantee holds),
  `run_polish.py`. **99 tests**, ruff clean. [[0009-phase6b-local-search-polish]].
- **The adversarial review workflow (Ultracode, 5 lenses → verify)** caught **1 real bug**: check_feasible
  checked the T_max return only on the final edge while the env checks after EVERY customer → under asymmetric OSM
  polish could return an env-infeasible solution. Fixed (a per-customer return) + 2 regression tests (the oracle + the money path).
- **Static** (to convergence, budget 30000ms): the polished portfolio **631.6€**, the gap to OR-Tools **+3.4%**
  (it was +25.4% in Step 3) — the largest shift of the phase. **Dynamic** (0004): RL+polish 827.3 vs greedy 851.5,
  the guarantee **0/25**, latency **689ms<1s**.
- **The key lesson (honestly)**: **polish ERASES the neural constructor's advantage** — greedy→652.2,
  RL→658.9, sample-K→650.9 converge into ~650-659 (the RL edge is +1.0%, RL is even slightly worse). The win =
  classic local search, reachable from greedy → it **does NOT prove the GNN+RL thesis**. The Step 3 asymmetry one
  level deeper (there multistart carried it, here polish). The dynamic −2.8% is a confound (vs unpolished greedy).
- **The convergence lesson**: 8000ms was budget-bound (portfolio 643.2); a probe on the heavy seed1 (converges in
  ~16s) → re-run at 30000ms (631.6, converged). Provenance: `results/polish_summary.json` (outside git).
- 19:30: session ended (auto)

## Checkpoint 21:45

**Step 3.5 committed + the vault/memory updated (/save).**

- Commits `1aeb964` (feat: the Step 3.5 code) + `49f7de4` (docs: 0009 + hot/daily). pytest 99, ruff clean,
  0 artefacts in the diff. Not pushed (only a commit was requested).
- Vault: [[0009-phase6b-local-search-polish]] + hot.md curated (What's Hot/Next) + this section.
- **Native memory**: a durable feedback lesson was recorded, "the honest asymmetric verdict" (isolate the
  contribution of a new lever; on this project the classics dominate quality, the RL contribution = latency) — a
  cross-decision synthesis of Steps 3/3.5, wider than a single decision.
- Left uncommitted (not Step 3.5): `.vault-state.json`, `daily 20/21`, the `0006` refit section,
  `index.md` (auto-generated). Awaiting a separate `docs(vault): checkpoint` or a push on request.
- 19:38: session ended (auto)
- 19:38: session ended (auto)
- 19:55: session ended (auto)
- 20:03: session ended (auto)
- 21:15: session ended (auto)
- 21:22: session ended (auto)
- 21:41: session ended (auto)

## Ablation 0010 + the Path B pre-registration/code (21:42)

**The ablation (the RL latency niche) built/measured/committed; Path B pre-registered + written + smoked.**

- **Ablation [[0010-phase6b-ablation-latency-niche]]** (NO training): extracted `run_dynamic.iter_events`
  (the same 0004 stream), `run_ablation.py` — 4 systems under an end-to-end budget of {50,100,200,500}ms, one
  scorer, a budget decomposition, a win profile. **There is NO niche:** a single RL decode is WORSE than greedy (865.5 vs
  851.5, 6/25) AND slower (18 vs 7ms); under a tight budget `rl_polish` loses to `greedy_polish`
  (an anti-niche); polish levels things out as the budget grows. **The open question of 0009 is closed negatively.**
- **Path B — the PRE-REGISTRATION [[0011-pathB-residual-curriculum-prereg]]** (the gate BEFORE the code, 3 prereg
  commits, the git timestamp = no tuning): the gate = a single decode beats greedy over 25 events (median Δ<0
  AND wins>12/25); kill patience=15/≤36h/one attempt. 2 edits of the prereg BEFORE the code (held out BY SEED, not
  node_id — a shared real pool; the base = `InstanceSampler(62,62)` cached, since `generate_instance` reloads in
  ~4s) — the gate itself is unchanged.
- **The Path B code:** `residual_curriculum.py` — the key: `residual_instance` = a fresh CVRPTW → POMO **without
  changes** (feasible_starts = K next nodes); `ResidualPOMOTrainer` (a 50/50 anti-forget mix,
  selection best-by-val-residual = **the gate metric**). `pomo._batch_instance_env` (a surgical extraction),
  `for_residual`, `train_residual.py --smoke/full`. **9 tests enforce the prereg, pytest 112, ruff clean.**
- **Smoke (the mechanism, both axes):** residual↓ 271.3→269.6, full stable 232.1→231.8 (anti-forget holds),
  |g|>0, entropy/freshness alive. **Pre-flight (advisor, the 48 pool):** val-RES **+3.07%** CONCORDANT with
  the gate **+1.64%** (the smoke "−1.9%" = noise at n=6) → the selector is directionally valid → **the 36h run IS worth it**.
- **The commits are pushed** (`origin/main` @ c26f6ae): 3× prereg 0011, the Path B code (`d169002`), 0010 (`c26f6ae`).
  Next: the server per [[train-on-server]] (`TRAIN=train_residual.py`) → the gate → **0012** (pass/fail, no retry).
- 21:42: session ended (auto)
- 21:45: session ended (auto)
- 21:53: session ended (auto)
- 21:57: session ended (auto)
- 22:30: session ended (auto)
- 22:55: session ended (auto)
- 23:14: session ended (auto)
- 23:34: session ended (auto)
- 23:55: session ended (auto)
