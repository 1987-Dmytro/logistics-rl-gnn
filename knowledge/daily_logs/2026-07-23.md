---
type: daily_log
date: 2026-07-23
tags: [daily]
---

# 2026-07-23

## Summary

A double day: **closing the research arc** (Phase 6b + the benchmarks) + assembling a publishable
form (Phase 9) + **the human-readable layer of Phase 8** (route sheet, demo, the presentation frame).

- **Path B — the verdict [[0012-pathB-residual-verdict]] (FAIL, the thesis is CLOSED):** the pre-registered
  gate of 0011 was not taken (median Δ **+14.84€**, **7/25**; it needed <0 AND >12). The training was healthy
  (early-stop ep48, selection ep33, val-FULL stable → anti-forget held), but the win on the single-event val pool
  did not transfer to the multi-event gate. One attempt, no retry — as fixed BEFORE the run. The thesis
  "GNN+RL delivers QUALITY" on the real Augsburg is closed negatively. Commit `a174489`.
- **Phase 8 — visualisation + the final metrics (the LinkedIn case):** the before/after map (+folium), 2 re-plan
  GIFs (a jam/a breakdown), the training curves, `final_metrics` (the parity guard 631.6€=0009). The honest
  table: costs **−23.5%** vs greedy, vehicle-hours on duty **−39.6%** (window idling, NOT driving —
  distance is ~flat), **+3.4%** vs OR-Tools, reaction **×2.9**. Two advisor fixes: latency separated
  (the neural floor ≠ deployment), "−40% time" → vehicle-hours on duty. Commit `a614509`, vault `67a75d6`.
- **Task #15 — the time-matched benchmark [[0013-time-matched-benchmark]] (the final):** OR-Tools was given THE
  SAME wall-clock (budgets {0.7,2,5,30}s) on identical instances. **Paired** (the same seeds → the instance σ
  cancels): OR reaches parity with the system's static quality (631.6€) by **<1s (6/10)**, and by **30s it
  beats it 8/10, median −18.6€/seed**. **The system has NO static advantage** in quality or in
  latency; the only durable edge of GNN+RL is **dynamics** (a re-plan in 689ms vs OR's 2001ms).
  The conflation quarantine: 631.6€ statics ≥30s ≠ 689ms dynamics (a residual, 827€). Commits `0d739dd` (code) +
  `086d75c` (vault 0013). An advisor fix: unpaired σ~44€ → paired discipline (median-Δ/wins, 0010).
- **Phase 9 — preparing for publication:** README (a full EN rewrite), LICENSE (MIT), pyproject metadata
  (SPDX license/classifiers/keywords/urls), `.github/workflows/ci.yml`, `docs/linkedin_post_draft.md`
  (2 variants × EN/RU). Assembled through an **Ultracode workflow**: gather (5 agents, the numbers tagged with
  their source+setting — anti-conflation) → written by hand → **an adversarial verify** (4 lenses). The verify
  caught: (1) my CI would have gone RED on a bare runner — `test_viz` imported matplotlib at collection,
  `test_local_search` had 8 snapshot tests without a guard → **both fixed, green confirmed by simulating
  a bare runner**; (2) LinkedIn Variant A hid the negative result → an honest caveat was added
  (quality = polish, not the network) EN+RU. Commits `36b8a72` (hygiene) + `219fd7b` (docs).
- **Phase 8 human-readable — the route sheet + demo + the presentation frame (3 commits):**
  - **The route sheet** (`fbf28d9`): `scripts/route_sheet.py` — a human-readable plan from THE SAME
    eval pipeline (the parity guard `cost == system_metrics per_seed`, the seed from the config), a per-vehicle
    timeline (windows/waiting/service/load), an honest on-time from the scorer. `enrich_names.py` — additively
    pulled the real German pharmacy names (**62/62 matched at 0.0 m**, `names.parquet`, matrices untouched).
    `viz_routes` — numbered folium stops + popups. The verify (5 lenses) caught 4: an ×140 overclaim in the
    dynamics appendix → rewritten with the greedy baseline ×2.9; the tests did not pin per-stop values; a viz
    snapshot desync; a hardcoded on-time of 100% → an honest computation.
  - **The demo** (`1e349cd`): `scripts/demo.py` — a 5-step narrative demonstration (morning → the plan with parity
    587.9€ → an event from the 0004 harness → a re-plan, portfolio vs OR-Tools vs greedy → the outcome), reusing all
    the scorers, static free-flow and congestion-residual never mixed. The verify caught a money-path bug in
    `_continue_old_plan` (an idx desync on urgent) → fixed.
  - **The demo upgrade** (`a1e3b0b`): renaming the outputs to self-describing ones
    (`1_morning_plan`/`2_incident_no_replan`/`3_incident_replan`) + floating headers (price/title/
    what-you-see) + **`compare.html`** (the screencast frame: an A/B/C table do-nothing/OR-Tools/the system
    in ONE residual world + two iframes B|C) + **road geometry** (every hop along real streets via
    `nx.shortest_path` over `graph.graphml`, cached; the old plan is a toggleable dashed layer; the incident
    zone labelled). **Honesty:** the static 587.9€ (map #1, the full day) ≠ the residual (#2/#3);
    the −82.8€ saving exists only inside the residual (do-nothing→replan, the same instance); OR@2s is shown honestly
    (826€, not converged; at ~30s it wins on quality — the durable verdict [[honest-asymmetric-verdicts]]);
    the latency comes from durable medians. **Adversarial verify (7 agents): 2 confirmed+fixed**
    (the price of #3 was taken from a separate rl run of `compare_replan` instead of the drawn `new` — polish is
    time-budgeted and could diverge → tied to `evaluate_solution(new)`; a vacuous numbering test —
    an identity idx → made non-identity with a hole), **3 rejected**. Visually confirmed in the browser
    (the frame renders, the toggle works, the polylines follow streets). `pytest 136 passed` · `ruff` clean.

## Decisions

- [[0012-pathB-residual-verdict]] — Path B FAIL, the "RL=quality" thesis is closed (pre-registration).
- [[0013-time-matched-benchmark]] — time-matched: the system has no static edge, only dynamics.
- (Phase 8 human-readable is an engineering layer, no separate dec-* was created; the lessons → git + the verdict memory.)

## Next

- **Before publication:** substitute the real GitHub path for `<user>`/`<repo-url>` (the README bibtex,
  `pyproject [project.urls]`, 4 LinkedIn blocks — marked `TODO(publish)`). CI activates after the push.
- **Push:** `main` is ahead of `origin` by **9** commits (a614509, 67a75d6, 0d739dd, 086d75c, 36b8a72,
  219fd7b, fbf28d9, 1e349cd, a1e3b0b) — not pushed (committing was requested, the push separately).
- Optional: record a screencast over `compare.html`; a LinkedIn post (after the author's review); optionally a
  deployment layer (the RL start as an anytime candidate in the portfolio).
- **The research arc is CLOSED** — do not open a new RL-"quality" branch without a new lever
  ([[honest-asymmetric-verdicts]]).

## Blockers

- none
